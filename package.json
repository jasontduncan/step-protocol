{
  "name": "step-cli",
  "version": "0.1.0",
  "description": "STEP — Structured Task Execution Protocol with a WorkDAG engine and CLI runner.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --test"
  },
  "bin": {
    "worktree": "./bin/worktree.js"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.4.0"
  }
}
